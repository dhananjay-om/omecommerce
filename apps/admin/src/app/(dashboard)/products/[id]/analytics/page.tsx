import { BarChart3 } from 'lucide-react';
import { InlineComingSoon } from '@/components/coming-soon';

export default function ProductAnalyticsPage() {
  return <InlineComingSoon icon={BarChart3} description="Per-product sales/views/conversion analytics aren't built yet — see the Analytics section in the sidebar for store-wide reporting." />;
}
