import { History } from 'lucide-react';
import { InlineComingSoon } from '@/components/coming-soon';

export default function ProductActivityPage() {
  return <InlineComingSoon icon={History} description="A change history/audit log for this product isn't built yet." />;
}
