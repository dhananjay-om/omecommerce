import { Star } from 'lucide-react';
import { InlineComingSoon } from '@/components/coming-soon';

export default function ProductReviewsPage() {
  return <InlineComingSoon icon={Star} description="Customer reviews and ratings aren't built yet — there's no review system in this store yet." />;
}
