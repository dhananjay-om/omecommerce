import { Braces } from 'lucide-react';
import { InlineComingSoon } from '@/components/coming-soon';

export default function ProductMetafieldsPage() {
  return <InlineComingSoon icon={Braces} description="Free-form custom key/value metafields aren't built yet — structured product data lives in Attributes on the Overview tab today." />;
}
