import { Share2 } from 'lucide-react';
import { InlineComingSoon } from '@/components/coming-soon';

export default function ProductChannelsPage() {
  return <InlineComingSoon icon={Share2} description="Per-channel visibility and listing status (marketplace/storefront/social) isn't built yet — this product is currently visible everywhere its status/visibility settings allow." />;
}
