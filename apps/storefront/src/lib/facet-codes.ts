/** Mirrors the backend's reserved facet codes (search/domain/ports.ts) — category/brand membership piggybacks on the generic {code, value} facet mechanism instead of dedicated fields. */
export const CATEGORY_FACET_CODE = '__category';
export const BRAND_FACET_CODE = '__brand';
